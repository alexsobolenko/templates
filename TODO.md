# TODO

## Auth / Mail / TDD Plan

- [x] Удалить `username` из проекта целиком:
  удалить из `src/Model/User.php`, `.docker/mysql/init/01_schema.sql`, `README.md` и всех связанных мест.
- [x] Подготовить базу для auth-flow:
  определить и внести минимальные изменения в модель `users` и SQL-схему для регистрации, подтверждения почты, восстановления пароля и обычной смены пароля.
- [x] Включить работу с PHP-сессией в правильной точке входа приложения.
- [x] Добавить mailer-сервис с двумя методами:
  отправка готового текста письма;
  отправка письма по шаблону и контексту.
- [x] Добавить отдельный layout для email-шаблонов и завести шаблоны писем отдельно от обычных web-view.
- [ ] Написать feature-тесты для `GET /register` и `POST /register`.
- [ ] Написать feature-тесты для `GET /confirm`.
- [ ] Написать feature-тесты для `GET /login` и `POST /login`.
- [ ] Написать feature-тесты для `GET /recover` и `POST /recover`.
- [ ] Написать feature-тесты для `GET /change-password` и `POST /change-password`.
- [ ] Написать feature-тесты для `POST /logout`.
- [ ] Написать feature-тесты для `GET /` для гостя и авторизованного пользователя.
- [ ] После feature-тестов реализовать auth-flow снизу вверх:
  session/auth support, mailer, контроллеры, views, маршруты, persistence-логику.
- [ ] Для логина запретить авторизацию пользователя с неподтверждённой почтой.
- [ ] Для recovery flow при запросе на смену пароля сбрасывать подтверждение почты и отправлять отдельное письмо с отличным текстом.
- [ ] Для `/confirm` после подтверждения почты авторизовывать пользователя и в сценарии регистрации, и в сценарии восстановления пароля.
- [ ] Для `/change-password` реализовать смену пароля по старому и новому паролю без сброса верификации почты.
